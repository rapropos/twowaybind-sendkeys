This testbed is intended to demonstrate a conflict between angular2's two-way binding and WebDriver's `sendkeys` function.

To run:

```
$ npm install
```

In one terminal:

```
$ node express.js
```

In another terminal:

```
$ protractor
```
